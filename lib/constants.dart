import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.fromRGBO(66, 135, 245, 1);
  static const primaryLight = Color.fromRGBO(100, 165, 250, 1);
  static const primaryDark = Color.fromRGBO(30, 100, 230, 1);

  static const secondary = Color.fromRGBO(255, 87, 34, 1);
  static const secondaryLight = Color.fromRGBO(255, 138, 80, 1);
  static const secondaryDark = Color.fromRGBO(216, 67, 21, 1);

  static const background = Color.fromRGBO(245, 245, 245, 1);
  static const surface = Color.fromRGBO(255, 255, 255, 1);

  static const textPrimary = Color.fromRGBO(33, 33, 33, 1);
  static const textSecondary = Color.fromRGBO(117, 117, 117, 1);

  static const accent = Color.fromRGBO(255, 193, 7, 1);
}
